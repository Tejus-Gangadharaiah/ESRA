enum scaredState(START, SK1, SK2, RETREAT);

enum scaredState = START;

time_t seconds = time(NULL);

int scared(void){
  switch (scaredState){
    case START:
      
      break;
    case SK1:
      
      break;
    case SK2:
      
      break;
    case Retreat:
      
      break;
    default:
      ;
      
      
  }
  return 0;
}
